#!/bin/bash
# {{project_name}} 停止开发环境脚本 (Linux/macOS)
# 使用方法: ./scripts/stop-dev.sh

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

echo "停止 {{project_name}} 开发环境..."

cd "$PROJECT_DIR"
docker-compose down

echo "开发环境已停止"
