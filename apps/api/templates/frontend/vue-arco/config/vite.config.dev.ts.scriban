import { mergeConfig } from 'vite';
import baseConfig from './vite.config.base';

export default mergeConfig(
  {
    mode: 'development',
    server: {
      open: true,
      host: '0.0.0.0',
      port: 3000,
      proxy: {
        '/api': {
          target: '{{ api_url }}',
          changeOrigin: true,
        },
      },
    },
  },
  baseConfig
);
